# ============================================================================
# Variables de Entorno - Frontend (React + Vite)
# ============================================================================
# Copia este archivo a .env y ajusta los valores según tu entorno
# IMPORTANTE: Las variables deben comenzar con VITE_ para ser accesibles en el código

# ============================================================================
# Keycloak - Autenticación
# ============================================================================
VITE_KEYCLOAK_HOST=localhost
VITE_KEYCLOAK_URL=http://localhost:8180
VITE_KEYCLOAK_REALM=datum-travels
VITE_KEYCLOAK_CLIENT_ID=datum-travels-frontend

# ============================================================================
# API Backend - URLs de servicios
# ============================================================================
# URL del backend principal (Quarkus - puerto 8081)
VITE_API_URL=http://localhost:8081/api

# URL del servicio OCR (Quarkus OCR - puerto 8080)
VITE_OCR_API_URL=http://localhost:8080/api

# ============================================================================
# Configuración de Proxy para Desarrollo
# ============================================================================
# Estas variables las usa vite.config.ts para configurar el proxy reverso
# Cambiar solo si ejecutas los backends en puertos diferentes
VITE_PROXY_BACKEND=http://localhost:8081
VITE_PROXY_OCR=http://localhost:8080
